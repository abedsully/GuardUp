import SwiftUI

struct GetHelpView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Get Help View")
            }
        }
        .navigationViewStyle(.stack)
    }
}

