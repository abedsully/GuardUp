import SwiftUI

struct JournalView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("This is journal view")
            }         
        }
        .navigationViewStyle(.stack)
        
    }
}
