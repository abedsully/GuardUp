import SwiftUI

struct ResourcesView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("This is resources view")
            }
        }
        .navigationViewStyle(.stack)
        
    }
}
