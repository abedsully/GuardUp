import SwiftUI

struct EmotionRecognitionView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("This is emotion recognition view")
            }
        }
        .navigationViewStyle(.stack)
    }
}
